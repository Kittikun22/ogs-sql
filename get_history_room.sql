SELECT CONCAT(users.name_prefix, ' ', users.first_name, ' ', users.last_name) AS fullname,  start_date, end_date, room_name FROM bookings INNER JOIN rooms ON bookings.rooms_id = rooms.id INNER JOIN users ON bookings.users_id = users.id
